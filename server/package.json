{
  "name": "wavelength-server",
  "version": "1.0.0",
  "description": "Backend API for the Wavelength music app — auth, playlists, likes (cloud sync)",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider node src/index.js",
    "dev": "NODE_OPTIONS=--openssl-legacy-provider node --watch src/index.js",
    "dev:mem": "node scripts/dev-mem.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "nodemailer": "^9.0.1",
    "resend": "^6.14.0",
    "sharp": "^0.35.1"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0"
  }
}
